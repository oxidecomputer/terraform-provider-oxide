---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "oxide_vpc_internet_gateway_ip_address_attachment Resource - terraform-provider-oxide"
subcategory: ""
description: |-
  This resource manages the attachment of an IP address to a VPC internet gateway.
---

# oxide_vpc_internet_gateway_ip_address_attachment (Resource)

This resource manages the attachment of an IP address to a VPC internet gateway.

## Example Usage

```terraform
resource "oxide_vpc_internet_gateway_ip_address_attachment" "example" {
  gateway_id  = "f5660a9f-962e-4c00-a6dc-638256ae1d4e"
  address     = "198.51.100.47"
  name        = "my-address-attachment"
  description = "an IP address attached to my internet gateway"
  timeouts = {
    create = "1m"
    read   = "1m"
    delete = "1m"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `address` (String) IP address to attach to the internet gateway.
- `description` (String) Description for the IP address attachment.
- `gateway_id` (String) ID of the VPC internet gateway.
- `name` (String) Name of the IP address attachment.

### Optional

- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) Unique identifier of the IP address attachment.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import ID is the format `${INTERNET_GATEWAY_ID}/${ATTACHMENT_ID}`.
terraform import oxide_vpc_internet_gateway_ip_address_attachment.example f5660a9f-962e-4c00-a6dc-638256ae1d4e/8bb5e081-4528-4d3d-a796-26f51f98d598
```
