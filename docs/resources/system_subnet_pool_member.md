---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "oxide_system_subnet_pool_member Resource - terraform-provider-oxide"
subcategory: ""
description: |-
  This resource manages a subnet pool member using the system API.
---

# oxide_system_subnet_pool_member (Resource)

This resource manages a subnet pool member using the system API.

## Example Usage

```terraform
resource "oxide_system_subnet_pool_member" "example" {
  subnet_pool_id    = "3e2c6e84-bed8-4c94-afc3-1032082d6a90"
  subnet            = "192.0.2.0/24"
  min_prefix_length = 24
  max_prefix_length = 28
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `subnet` (String) Subnet CIDR to add to the pool.
- `subnet_pool_id` (String) ID of the subnet pool.

### Optional

- `max_prefix_length` (Number) Maximum prefix length for allocations from this subnet. Defaults to 32 for IPv4 and 128 for IPv6.
- `min_prefix_length` (Number) Minimum prefix length for allocations from this subnet. Defaults to the subnet's prefix length.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) Unique, immutable, system-controlled identifier of the subnet pool member.
- `time_created` (String) Timestamp of when this subnet pool member was created.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import ID is the format `${SUBNET_POOL_ID}/${ID}`.
terraform import oxide_system_subnet_pool_member.example 3e2c6e84-bed8-4c94-afc3-1032082d6a90/9e199e45-01a6-43d3-8bc3-5b27726e67a6
```
