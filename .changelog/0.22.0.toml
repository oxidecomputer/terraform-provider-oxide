[[breaking]]
title = "`oxide_switch_port_settings`"
description = "The `oxide_switch_port_settings` resource has been removed."

[[breaking]]
title = "`oxide_subnet_pool`"
description = "This resource will be removed in version v0.25.0 of the provider. Use Terraform's `moved` functionality to move to the `oxide_system_subnet_pool` resource."

[[breaking]]
title = "`oxide_subnet_pool_member`"
description = "This resource will be removed in version v0.25.0 of the provider. Use Terraform's `moved` functionality to move to the `oxide_system_subnet_pool_member` resource."

[[breaking]]
title = "`oxide_subnet_pool_silo_link`"
description = "This resource will be removed in version v0.25.0 of the provider. Use Terraform's `moved` functionality to move to the `oxide_system_subnet_pool_silo_link` resource."

[[features]]
title = "New data source"
description = "`oxide_system_ip_pool`"

[[features]]
title = "New data source"
description = "`oxide_system_subnet_pools`"

[[features]]
title = "New data source"
description = "`oxide_current_user`"

[[features]]
title = "New resource"
description = "`oxide_vpc_internet_gateway_ip_address_attachment`"

[[features]]
title = "New resource"
description = "`oxide_vpc_internet_gateway_ip_pool_attachment`"

[[features]]
title = "New resource"
description = "`oxide_system_silo`"

[[enhancements]]
title = "`oxide_silo`"
description = "This resource will be removed in version v0.25.0 of the provider. Use Terraform's `moved` functionality to move to the `oxide_system_silo` resource."

[[enhancements]]
title = "`oxide_instance`"
description = "The oxide_instance resource now exposes a read-only `attached_external_ips` attribute, which lists attached floating, ephemeral, and SNAT external IP addresses. For Terraform-managed instances, this attribute can replace the `oxide_instance_external_ips` data source. [#841](https://github.com/oxidecomputer/terraform-provider-oxide/pull/841)"

[[features]]
title = "New resource"
description = "`oxide_system_subnet_pool`"

[[features]]
title = "New resource"
description = "`oxide_system_subnet_pool_member`"

[[features]]
title = "New resource"
description = "`oxide_system_subnet_pool_silo_link`"

[[enhancements]]
title = "`oxide_silo_saml_identity_provider`"
description = "The `idp_metadata_source` and `signing_keypair.private_key` attributes are now write-only. [#819](https://github.com/oxidecomputer/terraform-provider-oxide/pull/819)"

[[enhancements]]
title = "`oxide_silo_saml_identity_provider`"
description = "The `oxide_silo_saml_identity_provider` resource can now be imported. [#819](https://github.com/oxidecomputer/terraform-provider-oxide/pull/819)"

[[enhancements]]
title = "`oxide_ip_pool`"
description = "Added the read-only attributes `ip_version` and `pool_type`."

[[bugs]]
title = "`oxide_instance`"
description = "Previously, the provider would fail to detect a diff on the `external_ips` attribute of the `oxide_instance` resource when external IPs were detached from the instance out of band. These diffs are now detected correctly. [#842](https://github.com/oxidecomputer/terraform-provider-oxide/pull/842)"

[[bugs]]
title = "`oxide_floating_ip`"
description = "Previously, the `oxide_floating_ip` data source would return a null object and throw an uninformative error if the relevant floating IP didn't exist. These errors are now surfaced correctly, without manifesting as provider bugs. [#850](https://github.com/oxidecomputer/terraform-provider-oxide/pull/850)"
